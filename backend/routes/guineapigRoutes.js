const express = require('express');
const router = express.Router();

// Placeholder for guinea pig routes
router.get('/', (req, res) => {
    res.json({ message: 'Guinea pig routes' });
});

module.exports = router;
